#include <iostream>
#include <cmath>
using namespace std;
int main ()
{
    int age = 20;
    int money = 800000;

    if(age > 21 || money > 80){

            cout << "Welcome sire!" << endl;

    }
}

/* LOGICAL OPERATORS

- && AND logical operator, all conditions separated by AND have to be true.

- || OR logical operator, any of the conditions separated by OR have to be true.
*/
